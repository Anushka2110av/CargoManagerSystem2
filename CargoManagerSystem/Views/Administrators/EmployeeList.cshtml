@model IEnumerable<CargoClass.Employee>

@{

    ViewBag.Title = "EmployeeList";

}

<h2>EmployeeList</h2>

@*<p>

    @Html.ActionLink("Create New Employee", "CreateEmployee")*@

@*</p>*@
<p>@Html.ActionLink("Create New Employee", "CreateEmployee", "Administrators", null, new { @class = "btn btn-primary" })</p>
<table class="table">
    <tr>
        <th>

            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>

            @Html.DisplayNameFor(model => model.Role)
        </th>
        <th>

            @Html.DisplayNameFor(model => model.Email)
        </th>
        <th>

            @Html.DisplayNameFor(model => model.Address)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)

    {
        <tr>
            <td>

                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>

                @Html.DisplayFor(modelItem => item.Role)
            </td>
            <td>

                @Html.DisplayFor(modelItem => item.Email)
            </td>
            <td>

                @Html.DisplayFor(modelItem => item.Address)
            </td>
            <td>

                @*@Html.ActionLink("Edit", "UpdateEmployee", new { id=item.EmployeeId }) |

                    @Html.ActionLink("Details", "SearchEmployee", new { id=item.EmployeeId }) |

                    @Html.ActionLink("Delete", "DeleteEmployee", new { id=item.EmployeeId })*@

                @Html.ActionLink("Edit", "UpdateEmployee", "Administrators", new { id = item.EmployeeId }, new { @class = "btn btn-warning" })

                @Html.ActionLink("Details", "SearchEmployee", "Administrators", new { id = item.EmployeeId }, new { @class = "btn btn-warning" })

                @Html.ActionLink("Delete", "DeleteEmployee", "Administrators", new { id = item.EmployeeId }, new { @class = "btn btn-danger" })

            </td>
        </tr>

    }

</table>

<!-- Back to Dashboard -->
<!--<p>

    @Html.ActionLink("Back to Employee List", "EmployeeList", "Administrators", null, new { @class = "btn btn-secondary" })
</p>-->
<!-- 🔹 Back to Dashboard Button -->
<p>

    @Html.ActionLink("Back to Dashboard", "Index", "Administrators", null, new { @class = "btn btn-secondary" })
</p>
